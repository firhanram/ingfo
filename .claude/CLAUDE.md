# Testing 

Run related test before committing with: 
- pnpm --filter=[packageName] check-types
- pnpm --filter=[packageName] lint
- pnpm --filter=[packageName] format

# Commit Rules

See `COMMITS.md` for more details. 